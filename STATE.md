# STATE

Phase: DREAM loop after night-sprint-04 (cursor/night-sprint-04-3760)
Study days logged: 0 (SCOREBOARD study log still 0 until a real non-demo session ends or `pnpm scoreboard:sync` runs against real attempts). [DEMO] is not study.
Done: sprints 01–04; stretch demo/insights; DREAM cycle 1 hunt (newCap prefers leech/trap/gap topics); DREAM cycle 2 SCOREBOARD study-log sync from non-demo daily/diagnostic attempts (official table + weekly verdict preserved; simulation/demo excluded; auto-sync on session end only for data/app.db)
In flight: DREAM loop continues — next gap is in-session overconfidence note (miss with confidence ≥ 4)
Next: cycle 3 calibration on reveal; human QC of ten flags; real study after `pnpm db:reset` + ingest (no demo:seed); never verified=true without human
Counts: taxonomy 376; items 315; verified 0; PLACEHOLDER 0. Tests 50.
Risks: bank AI-unverified; hunt and scoreboard idle until real misses/sessions; mean mastery on the scoreboard uses live 0.6C+0.4R including any mixed attempts (demo:seed still refuses mix)
